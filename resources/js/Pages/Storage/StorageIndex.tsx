import LogisticLayout from "@/Layouts/LogisticLayout";

export default function StorageIndex() {
    return <div>StorageIndex</div>;
}

StorageIndex.layout = (page: any) => <LogisticLayout>{page}</LogisticLayout>;
